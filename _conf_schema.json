{
  "general": {
    "type": "object",
    "description": "基础开关与调试",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用插件",
        "default": true
      },
      "debug_log_conversions": {
        "type": "bool",
        "description": "在 debug 日志输出转换前后文本",
        "default": true,
        "hint": "便于排查转换方向和乱码是否正确。日志可能包含用户消息内容，不需要时可关闭。"
      },
      "debug_max_chars": {
        "type": "int",
        "description": "debug 日志中每段文本最多输出字符数",
        "default": 500,
        "min": 80,
        "max": 5000
      }
    }
  },
  "display": {
    "type": "object",
    "description": "回复显示格式",
    "items": {
      "include_original_text": {
        "type": "bool",
        "description": "奈亚语回复后附带原中文",
        "default": false,
        "hint": "开启后，中文转奈亚语、LLM 工具转奈亚语、爱丽丝触发回复会显示为：奈亚语（原中文）。"
      }
    }
  },
  "llm_tool": {
    "type": "object",
    "description": "LLM 工具：让人格在明确被要求时转换奈亚语",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用奈亚语转换 LLM 工具",
        "default": true
      },
      "description": {
        "type": "text",
        "description": "工具说明",
        "default": "奈亚语转换工具：convert_nyaya_language\n在用户明确要求把中文转换成奈亚语，或明确要求翻译/解读奈亚语乱码时使用。不要在用户只是闲聊、发送普通乱码梗、或没有提出转换需求时主动调用。\n调用时静默执行，不要说多余的话。",
        "hint": "这段说明会给 LLM 看，用来决定什么时候调用工具。"
      },
      "inject_usage_hint": {
        "type": "bool",
        "description": "在相关请求中提示 LLM 优先使用奈亚语工具",
        "default": true,
        "hint": "开启后，用户提到奈亚语/乱码/转换/翻译时，会提示 LLM 使用 convert_nyaya_language，而不是使用 Python 工具导入插件源码。"
      },
      "request_keywords": {
        "type": "list",
        "description": "触发 LLM 工具提示的关键词",
        "items": {
          "type": "string"
        },
        "default": [
          "奈亚语",
          "乱码",
          "转成",
          "转为",
          "转换",
          "翻译",
          "解读",
          "解释",
          "什么意思"
        ],
        "hint": "用户消息包含这些词时，插件会在 LLM 请求里补一句工具提示。"
      },
      "auto_mode_decode_min_score": {
        "type": "int",
        "description": "工具 auto 模式判定为奈亚语的最低分",
        "default": 6,
        "min": 1,
        "max": 100,
        "hint": "用户让工具自动判断方向时使用。分数越高，越不容易把普通文本误判成奈亚语。"
      }
    }
  },
  "commands": {
    "type": "object",
    "description": "显式命令：直接转换奈亚语",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用显式命令转换",
        "default": true,
        "hint": "这只响应明确命令，不会因为用户随手发乱码就自动翻译。"
      },
      "to_nyaya": {
        "type": "string",
        "description": "中文转奈亚语命令",
        "default": "/nyaya,/奈亚语",
        "hint": "多个别名用英文逗号分隔。"
      },
      "to_chinese": {
        "type": "string",
        "description": "奈亚语转中文命令",
        "default": "/unyaya,/解奈亚语",
        "hint": "多个别名用英文逗号分隔。"
      },
      "help": {
        "type": "string",
        "description": "命令帮助",
        "default": "/nyaya_help,/奈亚语帮助",
        "hint": "多个别名用英文逗号分隔。"
      }
    }
  },
  "alice_trigger": {
    "type": "object",
    "description": "爱丽丝里德尔触发：保留人格回复，只把回复转成奈亚语",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用爱丽丝里德尔触发",
        "default": true
      },
      "wake_llm": {
        "type": "bool",
        "description": "命中触发词时主动唤醒 LLM",
        "default": true,
        "hint": "开启后，即使群聊里没有 @ bot，只要命中触发词也会让 bot 按当前人格回复，然后插件再把回复转成奈亚语。"
      },
      "lossless_encode": {
        "type": "bool",
        "description": "爱丽丝触发回复使用可逆奈亚语",
        "default": false,
        "hint": "默认关闭，让爱丽丝回复更像原始 BLACKSOULS 乱码，避免出现大量 ・83、・EF 这类可逆字节标记。命令和 LLM 工具仍使用 codec.lossless_encode。"
      },
      "trigger_phrases": {
        "type": "list",
        "description": "触发词",
        "items": {
          "type": "string"
        },
        "default": [
          "爱丽丝里德尔",
          "爱丽丝·里德尔"
        ],
        "hint": "用户消息命中触发词后，bot 仍按原人格生成回复，只是在发送前把回复转换成奈亚语。"
      },
      "match_mode": {
        "type": "string",
        "description": "触发词匹配方式",
        "default": "contains",
        "options": [
          "contains",
          "exact"
        ],
        "labels": [
          "消息包含触发词",
          "整条消息等于触发词"
        ]
      }
    }
  },
  "codec": {
    "type": "object",
    "description": "奈亚语编码细节（通常不用改）",
    "items": {
      "lossless_encode": {
        "type": "bool",
        "description": "生成可逆奈亚语",
        "default": true,
        "hint": "开启后，无效 CP932 字节会被保存下来，能稳定转回中文。具体显示方式由 lossless_style 决定。"
      },
      "lossless_style": {
        "type": "string",
        "description": "可逆信息的保存方式",
        "default": "hidden",
        "options": [
          "hidden",
          "visible",
          "zero_width"
        ],
        "labels": [
          "隐藏 Tag 字符（推荐，手机更干净）",
          "可见十六进制（例如 ・84）",
          "旧版零宽字符（不推荐，手机可能露出占位符）"
        ],
        "hint": "hidden 视觉上更像原始乱码，并改用 Unicode Tag 字符保存字节，手机 QQ 上通常不会显示成奇怪占位符；visible 最稳但会显示十六进制标记；zero_width 是旧方案，部分手机会显示出来。"
      },
      "invalid_marker": {
        "type": "string",
        "description": "可逆奈亚语的无效字节标记",
        "default": "・",
        "hint": "hidden 模式下视觉上通常只看到这个字符；visible 模式会显示成类似 ・84；zero_width 旧模式可能在手机端露出不可见字符占位符。"
      },
      "unknown_chars": {
        "type": "string",
        "description": "翻译旧奈亚语时视为未知字节的字符",
        "default": "・?",
        "hint": "BLACKSOULS/老网页乱码里常把丢失字节显示成 ・ 或 ?。"
      },
      "uncertain_char": {
        "type": "string",
        "description": "无法确定的字显示为",
        "default": "□"
      },
      "source_encoding": {
        "type": "string",
        "description": "原文编码",
        "default": "utf-8",
        "hint": "一般不要改。",
        "invisible": true
      },
      "wrong_encoding": {
        "type": "string",
        "description": "错读编码",
        "default": "cp932",
        "options": [
          "cp932",
          "shift_jis"
        ],
        "labels": [
          "CP932 / Windows 日文",
          "Shift-JIS"
        ],
        "hint": "奈亚语效果来自 UTF-8 被日文编码错读。"
      }
    }
  }
}
